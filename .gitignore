# Reference clone: scratch4js is only for studying git-sb3 logic; it does not enter the repo.
/scratch4js/

# Reference material (external docs整理); not versioned.
/docs/reference/

# scratch-git / extension-git (the two packages in this monorepo)
node_modules/
*.sb3

# Working scratchpad (plan notes); must never enter history.
PLAN.md

# OS files
.DS_Store